import React from 'react';
import styles from "./index.module.scss";
import TitlePage from "../../components/UI/Title/TitlePage"

const index = () => {
    return (
        <div>
            <TitlePage title="Une erreur est survenue"/>
        </div>
    );
};

export default index;